<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/green_primary">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        app:cardCornerRadius="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="32dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Re:Buy"
                android:textSize="36sp"
                android:textColor="@color/green_primary"
                android:textStyle="bold"
                android:layout_gravity="center_horizontal"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/sign_up"
                android:textSize="24sp"
                android:textStyle="bold"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"/>

            <TextView style="@style/AuthLabel" android:text="@string/name" android:layout_marginTop="24dp"/>
            <EditText
                android:id="@+id/et_register_name"
                style="@style/AuthInput"
                android:hint="@string/enter_name"
                android:inputType="textPersonName"/>

            <TextView style="@style/AuthLabel" android:text="@string/phone_number" android:layout_marginTop="16dp"/>
            <EditText
                android:id="@+id/et_register_phone"
                style="@style/AuthInput"
                android:hint="@string/enter_phone"
                android:inputType="phone"/>

            <TextView style="@style/AuthLabel" android:text="@string/email" android:layout_marginTop="16dp"/>
            <EditText
                android:id="@+id/et_register_email"
                style="@style/AuthInput"
                android:hint="@string/enter_email"
                android:inputType="textEmailAddress"/>

            <TextView style="@style/AuthLabel" android:text="@string/password" android:layout_marginTop="16dp"/>
            <EditText
                android:id="@+id/et_register_password"
                style="@style/AuthInput"
                android:hint="@string/enter_password"
                android:inputType="textPassword"/>

            <TextView style="@style/AuthLabel" android:text="@string/confirm_password" android:layout_marginTop="16dp"/>
            <EditText
                android:id="@+id/et_confirm_password"
                style="@style/AuthInput"
                android:hint="@string/confirm_password_hint"
                android:inputType="textPassword"/>

            <Button
                android:id="@+id/btn_sign_up"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/sign_up"
                android:backgroundTint="@color/green_primary"
                android:layout_marginTop="32dp"
                android:paddingVertical="12dp"
                android:textColor="@android:color/white"/>

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="16dp"
                android:orientation="horizontal">
                <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="@string/already_have_account"/>
                <TextView
                    android:id="@+id/tv_sign_in"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text=" @string/sign_in"
                    android:textColor="@color/green_primary"
                    android:textStyle="bold"/>
            </LinearLayout>

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</androidx.constraintlayout.widget.ConstraintLayout>